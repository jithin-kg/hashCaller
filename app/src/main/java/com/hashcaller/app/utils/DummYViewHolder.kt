package com.hashcaller.app.utils

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class DummYViewHolder(private val view: View) : RecyclerView.ViewHolder(view) {
    fun bind(){

    }
}